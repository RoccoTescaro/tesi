from tests import *
from tools import *
from metrics import *
from music import *

if __name__ == "__main__":
    exit()

    #testButterflies()
    ## paths to data
    #PPaths = glob.glob('data/Scarlatti/real/**/*.mid', recursive=True)
    #QPaths = glob.glob('data/Scarlatti/fake/**/*.mid', recursive=True)
#
    #PSamples = np.array([extract_feature(p) for p in PPaths[:1]])
    #QSamples = np.array([extract_feature(q) for q in QPaths[:1]])


